name: ThinkBayes2
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Dev-only additions (applied on top of environment.yml)
  # notebook / CLI tools
  - jupytext
  - astor
  - typer
  # tests (make tests)
  - pytest
  - nbmake
  # Jupyter Book (jb/build.sh)
  - "jupyter-book<2"
  - ghp-import
  - sphinxcontrib-bibtex
